// TODO: Employee parent class with the following properties and methods:
//* name, id, email; getName(), getId(), getEmail(), getRole()-returns "Employee"
//! ADD VALIDATIONS

// new employee object constructor

// name via getName(data)

//  set id with getId(data)

// get email via getEmail(data)

// getRole() will return employee type

function Person(first, last, age, eye) {
    this.firstName = first;
    this.lastName = last;
    this.age = age;
    this.eyeColor = eye;
  }